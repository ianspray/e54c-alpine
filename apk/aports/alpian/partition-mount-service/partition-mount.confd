# SPDX-License-Identifier: MIT
# What is the hardware we want to examine ?
check_hardware_path="/dev/nvme0n1"

# Where do the mountpoints go ?
mount_point_root="/"

# Where the read-only lower mounts and overlay upper/work dirs live.
# This should stay on the ramdisk/tmpfs.
work_mount_point="/media"
