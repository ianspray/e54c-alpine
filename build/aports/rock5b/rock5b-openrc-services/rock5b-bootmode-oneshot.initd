#!/sbin/openrc-run
# SPDX-License-Identifier: MIT

name="rock5b-bootmode-oneshot"
description="Apply one-shot maintenance boot mode and restore immutable default"

depend() {
  need localmount
  before networking
}

start() {
  ebegin "Applying one-shot boot mode state"
  /usr/sbin/rock5b-bootmode-oneshot-apply >/dev/null 2>&1 || true
  eend 0
}
